<div class="_userinfo">
    <div class="gravatar">
        <img src="{{ $user->gravatar('100') }}" alt="{{ $user->name }}" class="gravatar"/>
    </div>
    <p class="lead">{{ $user->name }}</p>
</div>